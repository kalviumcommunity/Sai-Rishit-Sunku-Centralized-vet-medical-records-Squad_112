rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Placeholder security rules - teammate will provide production rules
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
