# Models Directory

This directory is reserved for Dart data models (e.g. Pet, Treatment, ClinicBranch, UserProfile, Vaccination, Document).

> **Note:** A teammate is separately developing the Firestore schema, security rules, and Dart model classes. Their model classes will be added here as they are provided. Do not invent custom schemas.
